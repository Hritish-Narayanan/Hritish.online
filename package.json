{
  "name": "hritish-online",
  "version": "1.0.0",
  "description": "Personal portfolio website for Hritish Narayanan",
  "scripts": {
    "build": "esbuild speed-insights.js --bundle --format=iife --outfile=dist/speed-insights.bundle.js --minify",
    "dev": "esbuild speed-insights.js --bundle --format=iife --outfile=dist/speed-insights.bundle.js --watch"
  },
  "keywords": ["portfolio", "personal-website"],
  "author": "Hritish Narayanan",
  "license": "ISC",
  "dependencies": {
    "@vercel/speed-insights": "^2.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
